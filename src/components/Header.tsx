import { Box, Typography } from "@mui/material";
const Header = () => {
  return (
    <Box>
      <Typography variant="h4" textAlign="center" color="#ccd6f6">
        Set goals, take action, achieve greatness.
      </Typography>
    </Box>
  );
};

export default Header;
